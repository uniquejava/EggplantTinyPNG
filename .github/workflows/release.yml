name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to build, for example v0.1.0'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    name: Build and publish macOS app
    runs-on: macos-latest
    env:
      RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Build archive
        env:
          ARCHIVE_PATH: ${{ runner.temp }}/EggplantTinyPNG.xcarchive
        run: |
          set -euo pipefail
          xcodebuild archive \
            -project EggplantTinyPNG.xcodeproj \
            -scheme EggplantTinyPNG \
            -configuration Release \
            -destination 'generic/platform=macOS' \
            -archivePath "$ARCHIVE_PATH" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_ALLOWED=YES \
            DEVELOPMENT_TEAM="" \
            PROVISIONING_PROFILE_SPECIFIER="" \
            SKIP_INSTALL=NO

      - name: Package DMG
        env:
          ARCHIVE_PATH: ${{ runner.temp }}/EggplantTinyPNG.xcarchive
          EXPORT_PATH: ${{ runner.temp }}/EggplantTinyPNG-release
          DMG_PATH: ${{ runner.temp }}/EggplantTinyPNG-release/EggplantTinyPNG-${{ env.RELEASE_TAG }}.dmg
        run: |
          set -euo pipefail
          APP_PATH="$ARCHIVE_PATH/Products/Applications/EggplantTinyPNG.app"
          STAGE="$EXPORT_PATH/dmg-stage"

          test -d "$APP_PATH"
          mkdir -p "$STAGE"
          cp -R "$APP_PATH" "$STAGE/"
          ln -s /Applications "$STAGE/Applications"
          hdiutil create -volname EggplantTinyPNG -srcfolder "$STAGE" -ov -format UDZO "$DMG_PATH"
          rm -rf "$STAGE"
          ls -lh "$DMG_PATH"

      - name: Prepare release notes
        env:
          NOTES_SRC: docs/releases/${{ env.RELEASE_TAG }}.md
        run: |
          set -euo pipefail
          if [[ -f "$NOTES_SRC" ]]; then
            cp "$NOTES_SRC" "${{ runner.temp }}/release-notes.md"
          else
            {
              echo "## EggplantTinyPNG ${{ env.RELEASE_TAG }}"
              echo
              echo "See commit history for changes."
            } > "${{ runner.temp }}/release-notes.md"
          fi

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: EggplantTinyPNG ${{ env.RELEASE_TAG }}
          body_path: ${{ runner.temp }}/release-notes.md
          files: ${{ runner.temp }}/EggplantTinyPNG-release/EggplantTinyPNG-${{ env.RELEASE_TAG }}.dmg
          generate_release_notes: true
